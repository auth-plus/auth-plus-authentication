extensions:
  health_check:
  pprof:
    endpoint: 0.0.0.0:1777
  zpages:
    endpoint: 0.0.0.0:55679

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
  hostmetrics:
    collection_interval: 10s
    scrapers:
      cpu:
      disk:
      load:
      filesystem:
      memory:
      network:
      paging:
  httpcheck:
    targets:
      - endpoint: 'http://localhost:13133/health/status'
        method: GET
      - endpoint: 'http://localhost:13134/health/status'
        method: GET
    collection_interval: 15s
  jaeger:
    protocols:
      grpc:
  postgresql:
    endpoint: database:5432
    transport: tcp
    username: root
    password: db_password
    databases:
      - auth
    tls:
      insecure: true
  prometheus/otelcol:
    config:
      scrape_configs:
        - job_name: 'otelcol'
          scrape_interval: 10s
          static_configs:
            - targets: ['0.0.0.0:8888']

processors:
  resourcedetection:
    detectors: ['system']
  batch:
    send_batch_size: 10000
    timeout: 10s

exporters:
  otlp/uptrace:
    endpoint: http://uptrace:4317
    tls: { insecure: true }
    headers: { 'uptrace-dsn': 'http://local_development_secret_token@uptrace:80?grpc=4317' }
  prometheusremotewrite/uptrace:
    endpoint: 'http://uptrace:80/api/v1/prometheus/write'
    tls: { insecure: true }
    headers: { 'uptrace-dsn': 'http://local_development_secret_token@uptrace:80?grpc=4317' }
  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      address: ':8888'
  pipelines:
    traces:
      receivers: [otlp, jaeger]
      processors: [batch]
      exporters: [otlp/uptrace, debug]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp/uptrace, debug]
    metrics/hostmetrics:
      receivers: [hostmetrics, postgresql, httpcheck]
      processors: [batch, resourcedetection]
      exporters: [otlp/uptrace, debug]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp/uptrace, debug]
    metrics/prom:
      receivers: [prometheus/otelcol]
      processors: [batch]
      exporters: [prometheusremotewrite/uptrace, debug]

  extensions: [health_check, pprof, zpages]